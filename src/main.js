/**
 * Entry point of our Vue application.
 * Creates the app instance and mounts it to the #app element.
 */
import { createApp } from 'vue';
import App from './App.vue';
import './style.css';

createApp(App).mount('#app');
